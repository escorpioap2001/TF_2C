apiVersion: v1
kind: Service
metadata:
  name: aparcamientos-api-service
  namespace: trabajo-final
spec:
  type: ClusterIP
  selector:
    app: aparcamientos-api
  ports:
    - port: 80
      targetPort: 80
